from setuptools import setup, find_packages

setup(name='dmenu-usbguard',
      version='1.1',
      # Modules to import from other scripts:
      packages=find_packages(),
      # Executables
      scripts=["dmenu-usbguard"])
